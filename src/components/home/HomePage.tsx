import React from "react";
import MoviesList from "./movies/MoviesList";

const HomePage = () => {
    return (
        <div>
            <MoviesList />
        </div>
    );
};

export default HomePage;
